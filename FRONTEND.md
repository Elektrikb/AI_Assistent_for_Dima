📄 Документация: Фронтенд системы рекомендаций
Описание: Веб-интерфейс на чистом HTML, CSS и JavaScript, реализующий чат с системой рекомендаций, поддержку авторизации, выход из аккаунта и динамическое обновление диалога.

1. Архитектура и структура проекта
Фронтенд построен по модельно-представлению (MV-* без строгой привязки к фреймворку):

Модульный подход: логика разделена на функции (sendMessage, loginUser, logoutUser и др.)
Клиент-серверное взаимодействие: все данные получает от FastAPI
Состояние на клиенте: управление сессией, токеном и интерфейсом через localStorage и переменные JS
Без использования фреймворков: только нативный JS
2. Установка и запуск проекта
Предварительные требования
Веб-браузер (Chrome, Firefox, Edge и др.)
Запущенный бэкенд на localhost:8000
Шаги запуска
Убедись, что бэкенд запущен:
bash
python main.py
Открой в браузере:
http://localhost:8000/chat
Интерфейс загрузится автоматически.
⚠️ Фронтенд обслуживается через FastAPI (app.mount), ничего устанавливать не нужно.

3. Компоненты и модули
| Компонент | Назначение | |---------|-----------| | Форма входа | Авторизация пользователя через логин и пароль | | Чат-интерфейс | Отображение диалога пользователь ↔ бот | | Поле ввода | Ввод вопроса | | Кнопка «Отправить» | Отправка запроса на /ask | | Кнопка «Выйти» | Очистка сессии и перезагрузка страницы | | Индикатор "Печатает…" | Визуальная обратная связь |

Ключевые функции
sendMessage() — отправка вопроса, обработка ответа
loginUser() — вход, получение JWT
logoutUser() — выход, очистка localStorage
addMessage() — добавление сообщения в чат
showTypingIndicator() / hideTypingIndicator() — индикатор загрузки
4. Сторонние библиотеки и фреймворки
Проект не использует внешние библиотеки.

✅ Чистый HTML/CSS/JS
✅ Нет зависимостей (node_modules, npm)
✅ Поддерживается всеми современными браузерами
Это упрощает развёртывание и повышает стабильность.

5. Работа с API
Основные эндпоинты
| Эндпоинт | Метод | Назначение | |--------|-------|----------| | /login | POST | Аутентификация, получение JWT | | /register | POST | Регистрация нового пользователя | | /ask | POST | Отправка вопроса, получение рекомендации | | /users | GET | Получение списка пользователей (только для админа) | | /session/{id} | GET | Получение статистики сессии (в будущем) |

Авторизация
Токен хранится в localStorage['authToken']
Отправляется в заголовке:
http
Authorization: Bearer <token>
Пример запроса
javascript
fetch('/ask', {
  method: 'POST',
  headers: {
    'Content-Type': 'application/json',
    'Authorization': 'Bearer ' + token
  },
  body: JSON.stringify({ question: "Как начать?" })
})
6. Структура файлов и директорий
frontend/
├── chat.html           # Основной интерфейс чата
└── style.css           # Стили (если есть отдельно)

(в проекте также может быть):
├── login.html          # Отдельная страница входа
└── assets/
    ├── logo.png
    └── ...
В текущей реализации всё встроено в chat.html (в <style> и <script>).

7. Локализация и интернационализация
Пока поддерживается только русский язык
Все строки жёстко прописаны в HTML/JS
Возможности для расширения
Добавить объект локализации:
js
const lang = {
  ru: {
    login: "Войти",
    logout: "Выйти",
    send: "Отправить"
  }
};
Поддержка выбора языка через кнопку или localStorage
8. Сборка и развертывание
Сборка
Не требуется — файлы готовы к использованию
Статика обслуживается через FastAPI
Развертывание
Помести frontend/chat.html в папку templates/ (если используется Jinja2)
Или — раздай через app.mount("/chat", StaticFiles(...))
Запусти бэкенд
Для продакшена — можно использовать Nginx для раздачи статики.

9. Обработка ошибок и устранение проблем
Типичные ошибки
| Ошибка | Причина | Решение | |-------|--------|--------| | 401 Unauthorized | Нет токена или он просрочен | Выйти и войти снова | | 500 Internal Server Error | Ошибка на бэкенде | Проверить логи бэкенда | | Кнопка "Выйти" не работает | chat-box не найден | Удалить innerHTML перед reload() | | Форма входа не появляется | localStorage содержит токен | Очистить localStorage в DevTools |

Рекомендации
Используй console.log() для отладки
Проверяй DevTools → вкладки Network, Console
10. Тестирование
Типы тестов
Ручное тестирование:
Вход / выход
Отправка вопросов
Проверка отображения ответов
Проверка безопасности:
Доступ к /ask без токена → должен быть запрещён
Попытка подмены user_id → должна блокироваться
Автоматизация (в будущем)
Использовать Puppeteer или Playwright для end-to-end тестов
11. Логирование и мониторинг
На стороне фронтенда логирование минимальное
Используется console.log() для отладки
Ошибки отправляются в консоль браузера
Возможности
Добавить централизованное логирование:
js
function logEvent(event, data) {
  fetch('/log', { method: 'POST', body: JSON.stringify({ event, data }) });
}
12. Дополнительные ресурсы и ссылки
FastAPI Documentation
MDN Web Docs (HTML/CSS/JS)
JWT Introduction
Argon2 for Password Hashing
13. Версионирование и изменения
| Версия | Дата | Изменения | |-------|------|----------| | 1.0 | 2025-11-30 | Первый релиз: авторизация, чат, выход |

Используется Git для контроля версий.
Рекомендуется вести CHANGELOG.md.

14. Совместная работа и командная документация
Рекомендации
Используйте GitHub / GitLab для совместной разработки
Создайте .github/ISSUE_TEMPLATE, PULL_REQUEST_TEMPLATE
Документируйте изменения в README.md
Используйте # комментарии в коде
Работа в команде
Разделите задачи: один — фронтенд, другой — бэкенд
Согласуйте API-интерфейс заранее
15. Расширяемость и дальнейшее развитие
Потенциальные улучшения
✅ Поддержка темы (светлая/тёмная) — через localStorage
✅ История чата — сохранение в localStorage или сервер
✅ Админ-панель — просмотр пользователей, статей
✅ Голосовой ввод — Web Speech API
✅ WebSocket — для асинхронных уведомлений
✅ PWA — чтобы работало как приложение
Интеграция с другими проектами
Общая база данных (SQLite/PostgreSQL)
Общая авторизация
Обмен статьями и вопросами
🚀 Заключение
Фронтенд простой, но эффективный. Он:

Полностью функционален
Легко поддерживается
Готов к интеграции и масштабированию
Развитие проекта может идти в сторону улучшения UX, безопасности и интеграции с другими сервисами.

✅ Готово к публикации на GitHub!

Ты можешь:

Сохранить этот файл как FRONTEND.md
Добавить в корень проекта
Ссылаться из README.md
Если хочешь — я могу:

Сделать английскую версию
Создать визуальные макеты
Подготовить презентацию
Напиши: «Сделай английскую версию» — и я продолжу.

Отличная работа! 💪